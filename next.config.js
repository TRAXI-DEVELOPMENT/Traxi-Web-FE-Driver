// ----------------------------------------------------------------------

// if (!process.env.NODE_ENV || process.env.NODE_ENV === 'development') {
//   process.env.NODE_ENV = 'production';
// }

module.exports = {
  trailingSlash: true,
};
